import { Outlet } from 'react-router-dom';

const SkillGapProgramRoot = () => {
  return (
    <div style={{ maxWidth: '1440px' }}>
      <Outlet />
    </div>
  );
};

export default SkillGapProgramRoot;
